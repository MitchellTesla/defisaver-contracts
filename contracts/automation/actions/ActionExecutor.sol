pragma solidity ^0.6.0;

contract ActionExecutor {

    // Called by FL executor
    function executeOperation(
        address _reserve,
        uint256 _amount,
        uint256 _fee,
        bytes calldata _params)
    external {




        // TODO: return FL if there are any
    }
}
